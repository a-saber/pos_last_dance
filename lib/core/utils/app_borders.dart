import 'package:flutter/material.dart';
class AppBordersSizes
{
  static const double fields = 5;
}
class AppBorders
{
  static const BorderRadius btn = BorderRadius.all(Radius.circular(AppBordersSizes.fields));
  static const BorderRadius form = BorderRadius.all(Radius.circular(AppBordersSizes.fields));
}